import { ObjPropertiesPipe } from './obj-properties.pipe';

describe('ObjPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
